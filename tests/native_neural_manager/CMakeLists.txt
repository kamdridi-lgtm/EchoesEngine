cmake_minimum_required(VERSION 3.20)
project(EchoesNativeNeuralManagerContract LANGUAGES CXX)
add_executable(echoes_native_neural_manager native_neural_manager_contract.cpp)
target_compile_features(echoes_native_neural_manager PRIVATE cxx_std_20)
target_include_directories(echoes_native_neural_manager PRIVATE ../../engine/include)
if(MSVC)
  target_compile_options(echoes_native_neural_manager PRIVATE /W4 /WX /permissive-)
else()
  target_compile_options(echoes_native_neural_manager PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()
