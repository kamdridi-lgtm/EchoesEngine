# Install a CUDA-compatible PyTorch build separately for the target machine.
# The provider imports torch at runtime only when a model id is configured.
diffusers>=0.39.0,<1.0
transformers>=4.49,<6.0
accelerate>=1.0,<2.0
safetensors>=0.4,<1.0
imageio>=2.34,<3.0
imageio-ffmpeg>=0.5,<1.0
