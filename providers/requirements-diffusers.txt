# Install a CUDA-compatible PyTorch build separately for the target machine.
# These versions are intentionally exact: the first real proof must be reproducible
# and must not silently cross a Transformers major-version boundary on restart.
diffusers==0.39.0
transformers==4.57.6
accelerate==1.14.0
safetensors==0.8.0
imageio==2.37.4
imageio-ffmpeg==0.6.0
